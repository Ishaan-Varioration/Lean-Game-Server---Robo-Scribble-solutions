Theorem:
example (i : ℕ): (-1 : ℤ)^i + (-1 : ℤ)^(i+1) = 0 := by

Response:
by_cases h : Even i
rw[Even.neg_pow, Odd.neg_pow]
ring
unfold Even at h
unfold Odd
choose r hr using h
use r
rw[hr]
ring
assumption
rw[not_even_iff_odd] at h
rw[Odd.neg_pow, Even.neg_pow]
ring
unfold Even
unfold Odd at h
choose k hk using h
use k + 1
rw[hk]
ring
assumption
