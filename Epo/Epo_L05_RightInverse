Theorem:
example : let f : ℤ × ℤ → ℤ × ℤ := fun (m, n) ↦ (m + n, m + 2 * n) HasRightInverse f := by

Response:
unfold HasRightInverse f
unfold Function.RightInverse
let g : ℤ × ℤ → ℤ × ℤ := fun (m, n) ↦ (2 * m - n, n - m)
use g
unfold Function.LeftInverse
intro x
unfold g
simp
ring
